AWSTemplateFormatVersion: '2010-09-09'
Transform: AWS::Serverless-2016-10-31
Description: SAM app with Lambda that fetches JSON and stores it in S3

Globals:
  Function:
    Timeout: 300
    Runtime: python3.13
    Architectures:
      - x86_64

Resources:
  FetchAndStoreFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: data-lake-energy-trade-bronze-nordpool
      CodeUri: src/
      Handler: handler.lambda_handler
      Environment:
        Variables:
          BUCKET_NAME: data-lake-energy-trade
          SSM_PARAMETER_NAME: /entsoe/security_token
      Policies:
        - S3WritePolicy:
            BucketName: data-lake-energy-trade
        - SSMParameterWithSlashPrefixReadPolicy:
            ParameterName: /entsoe/security_token
      Events:
        HourlyTrigger:
          Type: Schedule
          Properties:
            Schedule: cron(30 * * * ? *)
            Name: data-lake-energy-trade-bronze-entsoe-hourly
            Description: Run Lambda every hour at minute 30 UTC
            Enabled: true

Outputs:
  FunctionName:
    Value: !Ref FetchAndStoreFunction

  BucketName:
    Value: data-lake-energy-trade